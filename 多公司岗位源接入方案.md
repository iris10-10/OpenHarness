# OpenHarness 多公司岗位源接入方案

## 1. 目标

让求职 Agent 不只同步腾讯招聘岗位，而是可以接入多个公司的官方招聘来源，把岗位统一写入本地岗位库，并把公司信息投影到求职面板的公司库中。

目标效果：

- 支持多个公司岗位源并存。
- 各来源互不影响，失败不会拖垮其他来源。
- 岗位统一进入岗位搜索页。
- 公司信息统一进入公司库。
- 全程保持账号安全模式，不使用招聘网站账号、密码、Cookie 或浏览器登录态。

## 2. 当前基础

项目现在已经具备以下能力：

- `scraping.allowed_sources` 支持数组，可以登记多个来源。
- 内置安全获取器可以访问配置中的 HTTPS 白名单来源。
- 岗位同步会写入本地 `jobs.json`。
- 前端岗位搜索统一从 `/api/jobs` 读取本地岗位库。
- 每条岗位已经有来源字段：
  - `source_code`
  - `source_site`
  - `source_url`
  - `apply_url`
  - `provider`
  - `provider_record_id`
  - `fetched_at`
  - `last_seen_at`
  - `provenance_status`

因此，多来源接入不需要推翻现有结构，只需要扩展来源配置、字段适配和公司库投影。

## 3. 总体架构

```text
用户点击同步岗位
    |
    v
OpenHarness SafePublicJobsProvider
    |
    +-- 腾讯招聘官方 API
    +-- 字节跳动招聘官方 API/页面
    +-- 阿里招聘官方 API/页面
    +-- 美团招聘官方 API/页面
    +-- 其他已审查公司官方招聘源
    |
    v
来源白名单校验
    |
    v
字段标准化
    |
    +-- jobs.json
    +-- companies.json / 公司库
    +-- RAG jobs collection
    +-- RAG companies collection
    |
    v
Web 求职面板统一展示
```

## 4. 来源配置设计

每个来源都登记在 `settings.json` 的 `scraping.allowed_sources` 中。

示例：

```json
{
  "scraping": {
    "enabled": true,
    "account_safe_mode": true,
    "cache_freshness_hours": 6,
    "max_results": 30,
    "max_pages": 1,
    "max_details": 0,
    "allowed_sources": [
      {
        "source_code": "tencent_careers",
        "source_site": "腾讯招聘",
        "allowed_domains": ["careers.tencent.com"],
        "search_url_template": "https://careers.tencent.com/tencentcareer/api/post/Query?keyword={query}&pageIndex={page}&pageSize={limit}&language=zh-cn&area=cn",
        "default_company": "腾讯"
      },
      {
        "source_code": "bytedance_jobs",
        "source_site": "字节跳动招聘",
        "allowed_domains": ["jobs.bytedance.com"],
        "search_url_template": "待审查后填写",
        "default_company": "字节跳动"
      },
      {
        "source_code": "alibaba_careers",
        "source_site": "阿里巴巴招聘",
        "allowed_domains": ["talent.alibaba.com"],
        "search_url_template": "待审查后填写",
        "default_company": "阿里巴巴"
      }
    ]
  }
}
```

字段说明：

| 字段 | 说明 |
|---|---|
| `source_code` | 稳定来源 ID，不能重复 |
| `source_site` | 前端展示的来源名称 |
| `allowed_domains` | 允许访问和入库的域名白名单 |
| `search_url_template` | 搜索 URL 模板 |
| `default_company` | 当来源没有返回公司名时使用的公司主体 |

模板变量：

| 变量 | 说明 |
|---|---|
| `{query}` | 搜索关键词 |
| `{city}` | 城市 |
| `{limit}` | 单次数量上限 |
| `{page}` | 页码，当前安全策略只允许第一页 |

## 5. 多来源同步策略

### 5.1 同步顺序

同步时按 `allowed_sources` 的顺序逐个来源执行。

建议行为：

1. 每个来源单独请求。
2. 每个来源单独捕获错误。
3. 一个来源失败，不影响其他来源。
4. 最终生成整体同步报告和每个来源的子报告。

### 5.2 去重规则

岗位去重优先级：

1. `provider + provider_record_id`
2. `source_code + source_url`
3. `source_code + company + title + city + content_hash`

这样腾讯、字节、阿里即使岗位标题相同，也不会互相覆盖。

### 5.3 刷新策略

推荐默认：

- 普通查询缓存有效期：6 小时。
- 用户点击同步：手动触发一次。
- 每个来源最多返回 30 条。
- 每次同步只请求第一页。
- 失败来源标记为 `stale`，不删除历史岗位。

## 6. 字段标准化设计

不同公司 API 字段不一致，需要统一映射到 OpenHarness 岗位模型。

统一岗位字段：

```json
{
  "id": "job:openharness_public_jobs:provider_record_id",
  "title": "后端开发工程师",
  "company": "腾讯",
  "city": "深圳",
  "salary": "",
  "experience": "三年以上工作经验",
  "education": "本科",
  "description": "岗位描述",
  "source_code": "tencent_careers",
  "source_site": "腾讯招聘",
  "source_url": "https://careers.tencent.com/jobdesc.html?postId=xxx",
  "apply_url": "https://careers.tencent.com/jobdesc.html?postId=xxx",
  "provider": "openharness_public_jobs",
  "provider_record_id": "xxx",
  "published_at": "2026年09月19日",
  "fetched_at": "2026-09-21T07:44:06Z",
  "last_seen_at": "2026-09-21T07:44:06Z",
  "provenance_status": "verified",
  "status": "active"
}
```

每个来源需要建立字段映射表。

腾讯示例：

| OpenHarness 字段 | 腾讯字段 |
|---|---|
| `title` | `RecruitPostName` |
| `company` | `default_company` 或 `BGName` |
| `city` | `LocationName` |
| `experience` | `RequireWorkYearsName` |
| `description` | `Responsibility` + `Requirement` |
| `provider_record_id` | `PostId` |
| `published_at` | `LastUpdateTime` |
| `source_url` | `PostURL` |

## 7. 公司库投影设计

岗位同步成功后，从岗位记录中抽取公司信息，写入公司库。

### 7.1 公司记录字段

建议公司模型：

```json
{
  "id": "company:tencent",
  "name": "腾讯",
  "aliases": ["腾讯招聘", "Tencent"],
  "source_codes": ["tencent_careers"],
  "official_career_url": "https://careers.tencent.com",
  "job_count": 10,
  "cities": ["深圳", "北京", "上海", "广州"],
  "departments": ["CSIG", "WXG", "IEG"],
  "latest_job_published_at": "2026年09月20日",
  "last_synced_at": "2026-09-21T07:44:06Z",
  "status": "active"
}
```

### 7.2 投影规则

每次岗位同步后：

1. 按 `default_company` 或标准化后的 `company` 聚合。
2. 更新公司岗位数量。
3. 合并城市列表。
4. 合并部门或 BG。
5. 更新最近岗位发布时间。
6. 更新最近同步时间。

### 7.3 公司去重

公司去重优先级：

1. 配置中的 `company_id`。
2. 标准化后的公司名。
3. `source_code` 对应的 `default_company`。

后续可维护一张公司别名表：

```json
{
  "腾讯": ["腾讯", "Tencent", "腾讯科技"],
  "字节跳动": ["字节跳动", "ByteDance", "抖音集团"]
}
```

## 8. 前端展示设计

### 8.1 岗位搜索页

岗位卡片应展示：

- 岗位名
- 公司名
- 城市
- 经验
- 发布时间
- 来源网站
- 获取时间
- 原始岗位链接

来源标签示例：

```text
腾讯招聘
字节跳动招聘
阿里巴巴招聘
```

### 8.2 公司库页面

公司库应展示：

- 公司名称
- 岗位数量
- 覆盖城市
- 最近更新时间
- 来源
- 查看该公司岗位

点击公司后，可以进入公司详情：

- 公司基础信息
- 当前抓取到的岗位
- 岗位更新时间分布
- 常见岗位方向
- 已投递记录

## 9. 安全边界

必须坚持：

- 只接官方招聘 API、官方招聘页或明确授权来源。
- 不保存、不读取、不发送招聘网站账号、密码、Cookie、Token。
- 不使用自动登录。
- 不绕过验证码。
- 不自动投递。
- 不访问任意 URL。
- 不允许来源返回的链接跳出白名单域名。
- 不把岗位正文中的内容当作指令执行。

来源必须通过以下检查：

| 检查项 | 要求 |
|---|---|
| 是否官方来源 | 必须是公司官方招聘域名或授权来源 |
| 是否需要登录 | 不允许 |
| 是否需要 Cookie | 不允许 |
| 是否 HTTPS | 必须 |
| 是否有稳定岗位 ID | 推荐必须 |
| 是否有原始岗位链接 | 必须 |
| 是否能低频访问 | 必须 |
| 是否返回岗位更新时间 | 推荐必须 |

## 10. 推荐实施步骤

### Phase 1：多来源配置

目标：

- 保留腾讯来源。
- 增加 2-3 个经过验证的公司官方来源。
- 确保一个来源失败不影响其他来源。

任务：

1. 扩展 `SafePublicJobsProvider` 的多来源错误隔离。
2. 为每个来源增加字段映射。
3. 增加同步报告中的 per-source 统计。
4. 增加多来源同步测试。

### Phase 2：公司库投影

目标：

- 岗位同步后自动更新公司库。
- 公司页面能看到真实公司和岗位数量。

任务：

1. 增加 `companies.json` 存储。
2. 增加公司标准模型。
3. 在岗位同步后投影公司信息。
4. 增加 `/api/companies` 接口。
5. 前端公司库读取真实数据。

### Phase 3：更多公司源

目标：

- 接入更多国内公司官方招聘源。

候选来源：

| 公司 | 来源优先级 | 备注 |
|---|---|---|
| 腾讯 | 高 | 已验证官方 API |
| 字节跳动 | 高 | 需确认公开接口和字段 |
| 阿里巴巴 | 高 | 需确认公开接口和字段 |
| 美团 | 中 | 需确认是否稳定 |
| 京东 | 中 | 需确认是否稳定 |
| 快手 | 中 | 需确认是否稳定 |
| 百度 | 中 | 需确认是否稳定 |

### Phase 4：搜索体验增强

目标：

- 用户可以按公司、来源、城市、更新时间筛选。

任务：

1. 岗位页增加来源筛选。
2. 岗位页增加公司筛选。
3. 岗位页增加更新时间筛选。
4. 公司详情页跳转岗位搜索。

## 11. 验收标准

- 同步腾讯和至少另一个公司来源成功。
- 其中一个来源失败时，另一个来源仍能入库。
- 岗位搜索页能同时展示多个来源的岗位。
- 公司库能显示同步得到的公司。
- 每条岗位都有原始链接和来源名称。
- 不需要用户提供任何招聘网站账号、密码、Cookie。
- 测试覆盖 URL 白名单、字段标准化、去重、公司投影和失败隔离。

## 12. 当前下一步

建议下一步先做：

1. 保留腾讯招聘作为第一个稳定来源。
2. 再选择 2 个国内官方招聘源做审查。
3. 为每个来源写字段映射。
4. 增加公司库投影。
5. 在前端公司库页面展示真实公司数据。

