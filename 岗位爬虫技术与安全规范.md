# OpenHarness 岗位数据接入与账号安全技术规范

## 1. 目标与核心结论

本文档重新定义 OpenHarness 的岗位数据接入方案，重点是降低用户招聘网站账号、Cookie、IP 和隐私数据的风险。

核心结论：

> **岗位搜索优先使用不需要招聘网站账号的官方 API、授权岗位聚合服务或只读 Jobs MCP。MCP 只是接入协议，不会天然消除封号风险。**

必须遵守以下硬性原则：

1. OpenHarness 默认不保存、不读取、不转发招聘网站密码、Cookie、Token 或登录态。
2. OpenHarness 默认只做岗位搜索、岗位详情读取和来源链接展示，不做自动登录、自动投递和自动提交。
3. 用户搜索时优先查询本地缓存和 RAG，不直接实时访问招聘网站。
4. 所有岗位必须保留来源网站、原始岗位链接、采集时间和数据提供方。
5. 未经审查的 MCP Server、任意 URL 工具和任意浏览器工具不得进入生产工具白名单。
6. 数据源失败时降级到本地历史数据，不通过提高并发、换 IP 或绕过验证来“恢复成功率”。

## 2. 风险分级

### 2.1 推荐接入等级

| 等级 | 数据来源 | 是否接触招聘网站账号 | 风险 | 是否推荐 |
|---|---|---:|---|---|
| A | 官方 API 或正式授权岗位数据服务 | 否 | 低 | 首选 |
| B | 经过审查的远程只读 Jobs MCP | 否，或仅使用 Jobs 服务自己的 OAuth | 中低 | 推荐 |
| C | 本地 MCP Jobs 公开信息模式 | 否 | 中 | 仅低频、手动 |
| D | 本地爬虫使用招聘网站 Cookie/密码 | 是 | 高 | 禁止 |
| E | 自动登录、验证码绕过、自动投递 | 是 | 极高 | 禁止 |

### 2.2 对 MCP Jobs 的准确判断

使用 MCP Jobs 可能减少 OpenHarness 自己维护页面解析器的工作量，但要区分两种部署：

```text
本地 MCP Jobs
OpenHarness -> 本机 MCP Server -> 招聘网站
```

本地部署时，目标网站仍可能看到本机 IP、请求模式和浏览器行为。它仍然可能是本地爬虫，不能因为使用 MCP 就认为账号安全。

```text
远程或授权 Jobs MCP
OpenHarness -> HTTPS/MCP -> 第三方 Jobs 服务
```

如果第三方服务不要求用户提供招聘网站账号、密码或 Cookie，它通常比“本机登录态爬虫”更适合账号安全。但风险不会消失，而是转移为：

- 第三方服务的数据来源和授权是否可靠。
- 第三方服务是否保存搜索条件、简历或访问日志。
- 第三方服务是否会因来源网站策略变化而停止服务。
- 第三方服务是否会返回过期、重复或不完整岗位。
- MCP Server 是否会暴露超出岗位搜索所需的工具。

任何 Jobs MCP 在接入前都必须经过本文档规定的供应商审查和工具白名单审查。

## 3. 适用范围与明确禁止事项

### 3.1 允许的能力

系统可以：

- 搜索公开或已授权提供的岗位数据。
- 获取岗位详情。
- 读取并标准化岗位字段。
- 保存岗位来源、原始链接、发布时间和同步时间。
- 将岗位写入本地岗位库和 RAG `jobs` collection。
- 展示原始岗位链接，由用户自行打开并完成投递。

### 3.2 禁止的能力

系统不得：

- 要求用户把招聘网站密码交给 OpenHarness 或 MCP Server。
- 导入招聘网站 Cookie、Session、Authorization Token 或设备指纹。
- 自动登录、自动刷新登录态或自动切换账号。
- 绕过验证码、滑块、风控、付费墙、访问控制或 robots.txt。
- 通过代理池、IP 轮换、指纹伪装或高并发规避封禁。
- 自动提交简历、自动点击投递、自动发送私信或自动联系招聘者。
- 读取与岗位搜索无关的用户主页、私信、通讯录或社交关系。
- 使用未经审查的任意 MCP `fetch_url`、浏览器控制或 Shell 工具。
- 将岗位正文中的指令当作系统指令或工具调用执行。

## 4. 推荐总体架构

### 4.1 MCP 优先、凭据隔离

```text
用户搜索请求
      |
      v
OpenHarness Job Search Adapter
      |
      +-- 本地岗位缓存 / jobs.json
      |
      +-- 本地 RAG jobs collection
      |
      +-- 受信任的只读 Jobs MCP
              |
              +-- 官方 API
              +-- 授权岗位聚合服务
              +-- 供应商自有采集系统
      |
      v
来源验证与岗位标准化
      |
      +-- 本地岗位库投影
      +-- RAG 投影
      +-- Web/CLI/Agent 返回
      |
      v
用户打开原始链接并手动投递
```

### 4.2 关键隔离

OpenHarness 与岗位数据提供方之间应存在一个明确的适配层：

```text
MCP Client
    -> Tool Allowlist
    -> Input/Output Schema Validator
    -> Source URL Validator
    -> Provenance Enricher
    -> Local Job Store / RAG
```

MCP Server 返回的数据不能直接进入 Web 页面、模型上下文或投递流程，必须先经过：

1. Schema 校验。
2. 字段长度和资源限制校验。
3. 来源和 URL 校验。
4. HTML、脚本和控制字符清洗。
5. 外部内容安全标记。
6. 去重和更新时间判断。

## 5. MCP Server 接入规范

### 5.1 只读工具白名单

生产环境只允许接入经过审查的最小工具集合：

```text
list_sources
search_jobs
get_job_details
```

如果供应商没有这些精确名称，可以通过配置映射到等价的只读工具，但必须明确记录工具名称和参数 schema。

默认拒绝以下工具：

```text
login
logout
refresh_session
submit_application
apply_job
send_message
upload_resume
browser_control
fetch_url
execute_command
read_file
write_file
```

对于 MCP Server 声明的其他工具，OpenHarness 不得因为“工具看起来有用”而自动启用。

### 5.2 工具参数限制

搜索参数必须经过限制：

- 查询文本最大长度：建议不超过 200 个字符。
- 城市、岗位、技能字段使用有限长度字符串。
- 单次页数和结果数有硬上限。
- 禁止把完整简历作为默认搜索参数发送给第三方。
- 默认只发送岗位关键词、城市、薪资和经验等最小条件。
- 查询参数中不得包含手机号、邮箱、身份证号、密码或 Cookie。

岗位详情请求只能接收：

- 供应商返回的稳定岗位 ID。
- 经过域名白名单校验的来源 URL。

不得把用户提交的任意 URL 直接传给 MCP Server 的通用抓取工具。

### 5.3 MCP 传输和认证

远程 MCP 服务必须满足：

- 使用 HTTPS。
- 校验 TLS 证书。
- 校验 OAuth issuer、audience、scope 和过期时间。
- 优先使用授权码 + PKCE。
- 访问 Token 存入操作系统凭据存储，不写入项目文件。
- Token 不进入模型上下文、RAG metadata、日志和 Web 响应。
- 只申请岗位搜索所需的最小 scope。
- 支持撤销 Token 和断开供应商连接。

本地 MCP Server 必须满足：

- 以独立低权限用户或沙箱运行。
- 只开放必要的 stdio/本地端口。
- 不获得 OpenHarness 工作区、简历目录和配置目录的读取权限。
- 不获得 Shell、浏览器密码库和操作系统凭据访问权限。
- 不使用持久化的招聘网站登录态。

### 5.4 依赖和启动安全

生产环境禁止直接使用：

```text
npx -y some-package
```

原因是它会在运行时解析并安装未经固定版本审核的包。推荐：

1. 固定 MCP Server 包版本。
2. 使用 lockfile 和包完整性校验。
3. 由管理员审核源码、依赖、启动参数和网络权限。
4. 使用明确的本地安装目录或容器镜像。
5. 禁止 MCP Server 在运行时自行下载新代码。
6. 记录 MCP Server 版本、来源、哈希和审查日期。

如果使用类似 `mcp-jobs` 的开源项目，应把它视为第三方代码和潜在本地爬虫，而不是官方安全接口。必须先确认：

- 是否需要招聘网站账号或 Cookie。
- 请求是否由本机发往招聘网站。
- 是否会启动浏览器并保存用户数据目录。
- 是否有自动投递或消息工具。
- 依赖是否存在已知漏洞。
- 数据是否经过授权、是否提供来源链接。

## 6. 账号安全设计

### 6.1 账号安全模式

OpenHarness 应提供明确的账号安全模式，默认启用：

```text
account_safe_mode = true
```

启用后：

- 禁止配置招聘网站用户名、密码、Cookie 和 Token。
- 禁止启动带登录态的浏览器采集。
- 禁止使用 `submit_application`、`send_message` 等工具。
- 只允许连接不需要招聘网站登录的 Jobs MCP/API。
- 只允许返回岗位数据和原始链接。
- 数据同步默认手动或低频定时执行。

关闭该模式不应只是一个普通开关，必须：

- 由用户主动确认。
- 明确显示增加的风险。
- 记录审计事件。
- 仍然禁止验证码绕过和自动投递。

### 6.2 凭据流转规则

推荐凭据流：

```text
用户
  -> Jobs 服务自己的 OAuth 授权
  -> 操作系统凭据存储
  -> MCP Client
  -> Jobs 服务
```

禁止凭据流：

```text
招聘网站密码/Cookie
  -> OpenHarness 配置文件
  -> MCP 参数
  -> 模型上下文
  -> 日志或 RAG
```

即使供应商声称“需要 Cookie 才能获得更多岗位”，在账号安全模式下也必须拒绝。

### 6.3 日志脱敏

以下内容绝不能进入日志：

- 密码。
- Cookie。
- Authorization Header。
- OAuth refresh token。
- 完整简历。
- 手机号和邮箱。
- 浏览器用户数据目录。
- 包含认证参数的完整 URL。

错误日志只记录：

```text
供应商代码
工具名
HTTP 状态类别
错误类型
请求 trace id
时间
脱敏后的 URL 域名
```

## 7. 来源、投递链接和数据可信度

### 7.1 必需字段

每条岗位必须包含：

| 字段 | 说明 |
|---|---|
| `source_code` | 稳定来源代码，例如 `boss`、`lagou`、`provider_x` |
| `source_site` | 用户可读的来源名称 |
| `source_url` | 原始岗位详情链接 |
| `apply_url` | 用户手动投递时打开的链接 |
| `provider` | 返回该岗位的 MCP/API 服务 |
| `provider_record_id` | 提供方岗位 ID |
| `published_at` | 来源声明的发布时间 |
| `fetched_at` | OpenHarness 获取时间 |
| `last_seen_at` | 最近一次同步出现时间 |
| `provenance_status` | `verified`、`unverified`、`stale` 或 `invalid` |

没有可信来源链接的岗位可以用于内部检索，但不得显示为“可直接投递”。

### 7.2 来源验证

`source_site` 必须来自受信任的来源注册表，不能直接采用网页返回的品牌字符串。

`source_url` 和 `apply_url` 必须：

- 只使用 `https`，除非来源有明确且经过批准的 `http` 例外。
- 只允许来源注册表中的域名或批准子域名。
- 拒绝 `file://`、`data://`、`javascript:`、`ftp://` 等协议。
- 校验重定向后的最终域名。
- 拒绝回环地址、私有网段、链路本地地址和云元数据地址。
- 不允许隐藏的短链或未经审查的跳转域名。

### 7.3 用户投递流程

推荐流程：

```text
用户查看岗位
  -> OpenHarness 显示来源和更新时间
  -> 用户点击“打开原岗位”
  -> 浏览器打开 source_url/apply_url
  -> 用户自行登录和投递
```

OpenHarness 不读取用户在浏览器中的密码、Cookie 或投递结果，不自动点击提交按钮。

## 8. 本地存储与 RAG 接入

### 8.1 标准岗位模型

```json
{
  "id": "job:provider_x:abc123",
  "title": "Python 后端工程师",
  "company": "示例科技",
  "city": "杭州",
  "salary": "25-40K",
  "experience": "3-5年",
  "education": "本科",
  "description": "清洗后的岗位描述",
  "source_code": "provider_x",
  "source_site": "示例岗位服务",
  "source_url": "https://jobs.example.com/jobs/abc123",
  "apply_url": "https://jobs.example.com/jobs/abc123",
  "provider": "trusted_jobs_mcp",
  "provider_record_id": "abc123",
  "published_at": "2026-09-18",
  "fetched_at": "2026-09-21T01:00:00Z",
  "last_seen_at": "2026-09-21T01:00:00Z",
  "provenance_status": "verified",
  "status": "active"
}
```

### 8.2 写入原则

MCP 返回结果必须先经过标准化，然后分别投影到：

```text
标准岗位记录
    +--> jobs.json，供 Web 岗位列表使用
    +--> RAG jobs collection，供语义检索和匹配使用
    +--> CLI/Agent/Web 响应
```

要求：

- JSON 和 RAG 使用同一个稳定 ID。
- 岗位更新时替换旧正文和旧向量。
- 内容不变时只更新时间，不重复计算 embedding。
- 过期岗位先标记，不立即删除。
- 来源链接变更必须留下变更时间。
- MCP 服务不可用时保留最后一次可信快照，并标记 `stale`。

### 8.3 外部内容边界

岗位描述属于不可信外部内容：

- 删除脚本、样式、隐藏表单和控制字符。
- 不执行岗位正文中的命令、代码、模板表达式或链接指令。
- 送入模型时标记为“外部岗位资料”。
- 不允许岗位正文触发文件读取、Shell、消息发送或投递工具。
- 不把简历和岗位全文发送给第三方服务，除非用户明确确认且该服务有必要用途。

## 9. 查询、缓存和同步策略

### 9.1 搜索优先级

```text
本地新鲜缓存
    -> RAG jobs collection
    -> 受信任 Jobs MCP/API 增量同步
    -> 标准化、去重、写库
    -> 返回结果
```

“实时”应理解为“在可接受的新鲜度内同步”，而不是每次搜索都访问招聘网站。

推荐新鲜度：

- 普通岗位：6-24 小时。
- 用户主动刷新：允许手动触发一次受限同步。
- 同一来源连续失败：延长同步间隔。
- 用户搜索：不得因为没有结果就无限重试外部服务。

### 9.2 增量去重

去重优先级：

1. `provider + provider_record_id`
2. `source_code + canonical_url`
3. `source_code + company + title + city + content_hash`

不得只用岗位标题去重。

### 9.3 资源上限

每次 MCP 调用必须有：

- 最大结果数。
- 最大页数。
- 最大详情数。
- 最大响应字节数。
- 最大单次耗时。
- 最大同步总耗时。
- 最大重试次数。
- 最大并发数，默认 1。

所有上限都应在 OpenHarness 侧强制执行，不能只依赖 MCP Server 自己声明的限制。

## 10. MCP 供应商审查清单

接入前必须完成书面审查：

### 10.1 数据来源

- [ ] 是否说明岗位数据来源。
- [ ] 是否说明数据授权、许可或公开采集依据。
- [ ] 是否保留原始岗位 URL。
- [ ] 是否标注岗位发布时间和获取时间。
- [ ] 是否有删除、过期和纠错机制。

### 10.2 账号和隐私

- [ ] 是否不要求招聘网站密码。
- [ ] 是否不要求招聘网站 Cookie。
- [ ] 是否不接收用户简历全文作为搜索条件。
- [ ] 是否说明搜索日志保存周期。
- [ ] 是否支持删除用户数据。
- [ ] 是否不会把岗位搜索数据用于无关训练或营销。

### 10.3 MCP 工具

- [ ] 工具是否只有岗位搜索和详情读取。
- [ ] 是否没有自动投递和消息工具。
- [ ] 是否没有任意 URL 抓取工具。
- [ ] 是否没有 Shell、文件和浏览器控制工具。
- [ ] 输入和输出 schema 是否稳定。
- [ ] 是否支持超时、分页和结果上限。

### 10.4 软件供应链

- [ ] 版本是否固定。
- [ ] 包或镜像是否有完整性校验。
- [ ] 依赖是否经过漏洞扫描。
- [ ] 是否有维护者、版本记录和问题响应渠道。
- [ ] 本地运行时是否可以进行网络和文件权限隔离。

### 10.5 运行表现

- [ ] 是否支持低频请求。
- [ ] 是否有明确错误码。
- [ ] 是否能区分无结果、过期、拒绝和服务故障。
- [ ] 是否存在稳定的来源链接。
- [ ] 是否能在不登录招聘网站的情况下完成搜索。

任何关键项无法确认时，供应商只能进入实验环境，不能进入默认生产配置。

## 11. 本地 MCP Jobs 的安全运行规则

如果必须使用本地 MCP Jobs，采用以下限制：

1. 只采集公开页面。
2. 不配置招聘网站用户名、密码和 Cookie。
3. 不保存 Playwright 登录态、浏览器缓存或用户数据目录。
4. 在容器或低权限用户下运行。
5. 只允许访问批准的招聘网站域名。
6. 禁止任意 URL 访问和任意浏览器控制。
7. 单进程、低并发、低频率、有限页数。
8. 只做手动同步，不默认安装自动定时任务。
9. 采集结果必须包含来源 URL 和获取时间。
10. 失败时停止，不通过提高频率或更换 IP 继续尝试。

本地 MCP Jobs 只能作为低风险公开数据实验方案，不能作为“使用个人账号采集更多岗位”的方案。

## 12. 安全威胁与控制措施

| 威胁 | 结果 | 控制 |
|---|---|---|
| MCP Server 恶意或被接管 | 凭据泄露、文件读取 | 工具白名单、沙箱、最小权限 |
| 供应商要求招聘网站 Cookie | 账号被风控或盗用 | 账号安全模式直接拒绝 |
| MCP 返回恶意岗位正文 | 提示注入、工具误调用 | 外部内容隔离、工具权限隔离 |
| 投递链接跳转内网 | SSRF、钓鱼 | 域名白名单、重定向校验 |
| 第三方服务保存搜索条件 | 隐私泄露 | 最小化查询、供应商审查、删除机制 |
| 高频刷新 | IP 或服务账号被限制 | 本地缓存、低频同步、硬上限 |
| 页面结构变化 | 错误岗位、旧链接 | schema 校验、来源状态、固定样本回归 |
| 依赖供应链攻击 | 本地代码执行 | 版本固定、完整性校验、依赖扫描 |
| 岗位过期 | 用户投递失败 | `fetched_at`、`last_seen_at`、过期标记 |

## 13. 审计、监控和撤销

每次同步生成 `collection_run_id`，记录：

```text
run_id
provider
source_code
tool_name
query_digest
started_at
finished_at
fetched_count
inserted_count
updated_count
skipped_count
stale_count
failed_count
server_version
```

审计日志不得保存认证数据和完整敏感请求。

管理员或用户应能：

- 禁用某个 MCP Server。
- 撤销远程 MCP OAuth 授权。
- 清除本地岗位快照和同步状态。
- 查看最近同步失败原因。
- 查看岗位的来源和最后更新时间。
- 将某个来源标记为不可信并停止入库。

发现以下情况时，必须立即停止该来源：

- MCP Server 开始要求招聘网站 Cookie 或密码。
- 工具列表出现自动投递、消息发送、文件读取或 Shell 工具。
- 返回链接大量跳转到未批准域名。
- 出现异常高频请求或大量验证码。
- 服务商无法解释数据来源或隐私处理。
- 发现 Token、简历或用户信息出现在日志中。

## 14. 测试要求

### 14.1 MCP 合约测试

使用 mock MCP Server 验证：

- 只调用工具白名单。
- 错误 schema 会被拒绝。
- 过大的结果会被截断或拒绝。
- 分页和结果上限生效。
- MCP 失败时返回本地缓存。
- 来源字段和链接字段完整保留。

### 14.2 账号安全测试

至少覆盖：

- 配置文件中出现招聘网站密码时启动失败。
- 请求参数中出现 Cookie 或 Authorization 时被拦截。
- MCP 返回的 Token 不进入日志、RAG 和模型上下文。
- 自动投递工具被拒绝。
- 任意浏览器工具被拒绝。
- 本地 MCP Server 无法读取简历目录和工作区。
- 撤销授权后无法继续请求。

### 14.3 URL 和内容安全测试

至少覆盖：

- `file://`、`data://`、`javascript:` URL 被拒绝。
- 重定向到 localhost、私有网段和云元数据地址被拒绝。
- 未批准域名被拒绝。
- 岗位正文中的伪指令不会触发工具调用。
- Web 页面不会产生 XSS。
- HTML、脚本和控制字符会被清洗。

### 14.4 数据一致性测试

验证：

- 相同岗位重复同步不会重复写入。
- 岗位更新会替换旧 RAG chunk。
- Web `jobs.json` 与 RAG 使用相同稳定 ID。
- 失效岗位先标记为过期。
- MCP 服务不可用时保留并标记旧快照。

## 15. 上线验收标准

- [ ] 默认启用账号安全模式。
- [ ] 默认不使用招聘网站账号、密码、Cookie 或登录态。
- [ ] 只允许官方 API、授权服务或经过审查的只读 Jobs MCP。
- [ ] MCP 工具白名单已经固定。
- [ ] 禁止自动投递、消息发送、任意 URL、Shell、文件和浏览器控制工具。
- [ ] 远程连接使用 HTTPS 和最小 OAuth scope。
- [ ] 本地 Server 使用固定版本、lockfile、沙箱和最小权限。
- [ ] 不使用未固定版本的 `npx -y` 作为生产启动方式。
- [ ] 查询、岗位正文和错误日志完成脱敏。
- [ ] 所有岗位包含来源网站、原始链接和获取时间。
- [ ] URL 白名单和重定向校验生效。
- [ ] 本地缓存和 RAG 优先，搜索不默认实时访问招聘网站。
- [ ] 单次同步、重试、并发和响应大小均有硬上限。
- [ ] MCP 服务故障可以降级到本地历史数据。
- [ ] 用户可以撤销授权、禁用来源和清理本地数据。
- [ ] MCP 合约、账号安全、URL 安全和数据一致性测试通过。

## 16. 推荐实施顺序

1. 增加统一岗位模型：`source_code`、`source_site`、`source_url`、`apply_url`、`provider`、`fetched_at`。
2. 增加 MCP Client 适配层和工具白名单。
3. 增加账号安全模式，默认拒绝招聘网站凭据。
4. 增加远程 MCP/API 的 HTTPS、OAuth、scope 和 Token 管理。
5. 增加 URL 白名单、SSRF 防护、重定向校验和内容清洗。
6. 将岗位数据统一写入本地岗位库和 RAG。
7. 增加本地缓存、增量同步、过期标记和失败降级。
8. 在 Web、CLI 和 Agent 中展示来源网站、原始链接和更新时间。
9. 进行 mock MCP 合约测试和小规模灰度。
10. 只有完成供应商审查后，才允许启用具体 Jobs MCP。

## 17. 参考资料

- [Model Context Protocol Specification](https://modelcontextprotocol.io/specification)
- [Model Context Protocol Authorization](https://modelcontextprotocol.io/specification/draft/basic/authorization)
- [OpenHarness RAG architecture](E:/暑假任务/OpenHarness/docs/jobhunt/architecture.md)
- [OpenHarness job scraper implementation](E:/暑假任务/OpenHarness/src/openharness/rag/sources/job_scraper.py)
- [OpenHarness scraper base implementation](E:/暑假任务/OpenHarness/src/openharness/rag/sources/base.py)
- [OpenHarness RAG search tool](E:/暑假任务/OpenHarness/src/openharness/tools/rag_search_tool.py)

如果接入具体的第三方项目，例如某个开源 `mcp-jobs` Server，还必须把该项目的仓库地址、固定版本、依赖审查结果、权限需求和数据来源写入单独的供应商登记记录，不能仅凭项目名称或 MCP 协议本身视为可信。

